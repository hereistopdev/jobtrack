{
  "name": "jobtrack",
  "private": true,
  "description": "Root package for Vercel: installs serverless bridge + delegates to frontend/backend",
  "scripts": {
    "vercel-build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "serverless-http": "^3.2.0"
  }
}
